[project]
name = "stock-predictor"
version = "0.1.0"
description = "Stock prediction ML app"
requires-python = ">=3.10"

dependencies = [
    "streamlit",
    "yfinance",
    "pandas",
    "numpy",
    "scikit-learn"
]